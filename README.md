# Form_Validation
A simple HTML and JavaScript-based form validation project that validates user input fields such as User ID, contact number, password, and confirm password. It ensures all fields are filled, verifies a 10-digit numeric contact number, and checks password matching before allowing form submission.
